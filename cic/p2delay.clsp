(mod (INNER_PUZZLE DELAY_IN_SECONDS inner_solution)

  (include condition_codes.clib)

  (defun tweak_solution (DELAY_IN_SECONDS conditions)
    (c (list ASSERT_SECONDS_RELATIVE DELAY_IN_SECONDS) conditions)
  )

  (tweak_solution DELAY_IN_SECONDS (a INNER_PUZZLE inner_solution))
)
